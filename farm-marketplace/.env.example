# Copy to .env and fill in local values. Never commit .env.

# Backend API base URL (use your machine LAN IP for physical devices)
# Android emulator: http://10.0.2.2:5000/api
# iOS simulator:    http://localhost:5000/api
EXPO_PUBLIC_API_URL=http://localhost:5000/api

# Cloudinary — use an UNSIGNED upload preset only (safe for mobile clients).
# Do NOT put API secrets in the app. Create preset: Console → Settings → Upload → Upload presets.
EXPO_PUBLIC_CLOUDINARY_CLOUD_NAME=
EXPO_PUBLIC_CLOUDINARY_UPLOAD_PRESET=
